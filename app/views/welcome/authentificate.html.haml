!!!
.auth
  = form_with ulr:welcome_authentificate_path, local: true do |f|
    = f.label :username
    = f.text_field :username
    = f.label :password
    = f.password_field :password
    = f.submit "Submit"
-#.error
-#  = if @user.errors.any?
-#  %p 'AAAAAAA'