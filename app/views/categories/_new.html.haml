-#= form_with model: @category, local: true do |f|
-#  = f.label t(:'site.category.name')
-#  = f.text_field :name, id: 'category-name_field'
-#  = f.submit t(:'site.common.create'), id: 'category-create_button'
.form-group.container.mt-2
  = form_with model: @category, local: true do |f|
    = f.label t(:'site.category.name')
    = f.text_field :name, id: 'category-name_field'
    = f.submit t(:'site.common.create'), class: 'btn btn-primary', id: 'category-create_button'