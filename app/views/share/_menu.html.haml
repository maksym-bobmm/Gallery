%nav.navbar.navbar-expand-lg.navbar-dark.bg-secondary
  .navbar-collapse.order-0.text-center
    .navbar-brand Gallery
    %ul.navbar-nav.ml-auto
      %li.active.nav-item
        %a.nav-link(href=root_path) Главная

      %li.nav-item.dropdown
        = link_to "Categories", root_path, class: "nav-link dropdown-toggle", id: "navbardrop",
                    'data-toggle': "dropdown", 'aria-haspopup': "true", 'aria-expanded': "false"
        .dropdown-menu
          = link_to "category[:category].name", categories_path, class: 'dropdown-item'
          -#-rating_categories.each do |category|
          -#  = link_to "#{category.title.capitalize}", category_path(category), class: "dropdown-item"
          .dropdown-divider
          = link_to "All categories", categories_path, class: "dropdown-item"
      -#%li.nav-item.dropdown
      -#  %a.nav-link.dropdown-toggle#navbarDropdown(href='#' role="button" data-toggle='dropdown' aria-haspopup="true" aria-expanded="false") Categories
      -#  -#%a.nav-link(href=categories_path) Категории
      -#  .dropdown-menu(aria-labelledby="navbarDropdown")
      -#    - @categories_with_rating.first(5).each do |category|
      -#      = link_to category[:category].name, category_path(category[:category]), class: 'dropdown-item'
      %li.nav-item
        %a.nav-link(href=images_path) Все картинки
      %li.nav-item
        %a.nav-link(href=admin_root_path) Admin
      %li.nav-item

  .navbar-collapse.order-1
    %ul.navbar-nav.ml-auto
      %li.nav-item
      - if user_signed_in?
        = link_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'nav-link text-center text-uppercase'
      - else
        = link_to 'Sign In', 'users/sign_in', class: 'nav-link text-center'
